#include<bits/stdc++.h>
#include<boost/multiprecision/cpp_int.hpp>
using namespace std;
#define M 1000000007
#define fastio ios_base::sync_with_stdio(false);cin.tie(NULL);
using namespace boost::multiprecision;
#define int cpp_int

int32_t main(void)
{
    fastio
    int t;
    cin>>t;
    while(t--)
    {
            int n;
            cin>>n;
            int x=n;
            int sum=0;
            point:
            if(x!=0){
            sum=sum+x;
            x=x/10;
            goto point;
            }//new method source
            //cout<<sum<<endl;
            sum=sum;
            if(sum==0)
            cout<<n*10<<endl;
            else
            cout<<10*(n+1)-sum<<endl;
        
        
    }
    return 0;
}
