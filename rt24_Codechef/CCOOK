#include<stdio.h>

int main()
{
    int t;
    scanf("%d",&t);
    for(int i=0;i<t;i++)
    {
        int ar[5];
        for(int i=0;i<5;i++)
            scanf("%d",&ar[i]);
        int count1=0;
        for(int i=0;i<5;i++)
        {
            
            if(ar[i]==1 || ar[i]==0)
            {

                    if(ar[i]==1)
                        count1++;
            }
            else
            break;
            
        }
        if(count1==0)
                printf("Beginner");
            if(count1==1)
                printf("Junior Developer");
            if(count1==2)
                printf("Middle Developer");
            if(count1==3)
                printf("Senior Developer");
            if(count1==4)
                printf("Hacker");
            if(count1==5)
                printf("Jeff Dean");
            printf("\n");
        
        
        
    }
    
}

